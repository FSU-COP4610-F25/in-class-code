**Summation with different methods**: We compare scalability across a hand-written spin lock, a mutex, and atomic instructions. We use a Python script. For the three executables `atomic`, `mutex`, and `spin`, set `argv[1]` to 1, 2, 4, 8, and 16 and record the median of five runs. Plot with matplotlib, showing `atomic`, `mutex`, and `spin` as three lines. The x-axis is `argv[1]`, the y-axis is time with error bars.
