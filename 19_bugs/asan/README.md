**AddressSanitizer**: AddressSanitizer checks code correctness by having the compiler automatically insert memory-related assertions. For systems whose complexity exceeds human control, such as operating system kernels, this mechanism is essential. Another way to ensure correctness is to restrict the programming language so that certain classes of errors cannot be written by humans, for example, using languages with automatic memory management.
