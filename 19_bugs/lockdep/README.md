**lockdep**: Tracking the locking order for every individual lock is expensive. A more economical approach is to treat all locks initialized on the same line of code as “the same lock,” which greatly reduces the number of locks. This does sacrifice some detection accuracy. For example, in the dining philosophers problem, if all locks are initialized on the same line, we cannot distinguish their locking order.
