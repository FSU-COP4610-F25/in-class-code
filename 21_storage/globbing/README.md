**Globbing**: In `pstree` we implement a directory-tree traversal, and we almost always need a thin wrapper over low-level filesystem APIs, for example the very convenient [`glob()`](https://www.gnu.org/software/libc/manual/html_node/Calling-Glob.html).

