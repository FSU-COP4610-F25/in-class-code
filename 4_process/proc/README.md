**proc.c**: This example shows how to obtain basic process information. Besides using APIs such as `getpid` (a system call) the Linux `/proc` file system lets you use the file API to access metadata for the current process. You can read the process ID, status, command-line arguments, and working directory.

