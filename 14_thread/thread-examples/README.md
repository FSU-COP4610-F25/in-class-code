**Understanding thread behavior through a thread library:** We do not need to take the textbook at face value. We can verify it ourselves. For example, threads do share memory. We can also check that each thread has its own stack. Mark the stack regions that were accessed, and you will see an approximate range for each thread’s stack.

